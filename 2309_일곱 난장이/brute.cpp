#include <bits/stdc++.h>
using namespace std;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL), cout.tie(NULL);

    int a, b, c, d, e, f, g;
    for (a = 0; a < 9; a++) {
        for (b = 0; b < 9; b++) {
            for (c = 0; c < 9; c++) {
                for (d = 0; d < 9; d++) {
                    for (e = 0; e < 9; e++) {
                        for (f = 0; f < 9; f++) {
                            for (g = 0; g < 9; g++) {

                            }
                        }
                    }
                }
            }
        }
    }

    return 0;
}