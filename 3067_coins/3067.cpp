#include <bits/stdc++.h>
using namespace std;

int main() {
    int t;
    int n, i;
    int val, price;
    cin >> t;
    while(t--) {
        cin >> n;
        vector<int> coins;
        for(i=0; i<n; i++) {
            cin >> val;
            coins.push_back(val);
        }
        cin >> price;
        
    }
    return 0;
}